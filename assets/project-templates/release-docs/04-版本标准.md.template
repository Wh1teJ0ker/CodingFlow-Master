# 版本标准

> 本文件为模板，复制到目标项目 `docs/04-版本标准.md` 后替换占位符并按真实版本填写。

## 版本号语义

本项目采用[语义化版本](https://semver.org/)（Semantic Versioning），格式为 `MAJOR.MINOR.PATCH`：

- `MAJOR`：不兼容的 API 或行为变更。
- `MINOR`：向后兼容的新功能。
- `PATCH`：向后兼容的问题修复。

本项目只允许稳定版数字语义化版本，不使用 `rc`、`beta`、`alpha` 等预发布体系。应用版本写作 `1.2.0`，Git tag 写作 `v1.2.0`。

## 唯一版本源

| 项目类型 | 版本源 |
|---|---|
| Rust + Tauri | `src-tauri/tauri.conf.json` 的 `version` 字段 |
| Go + Wails | `wails.json` 的 `info.productVersion` |

应用版本、Git tag（去 `v` 前缀）、发布产物命名、版本文档目录必须一致。CI/Release 流程包含版本一致性检查，不一致时 fail。

## 版本状态口径

| 状态 | 含义 | 是否可发布 |
|---|---|---|
| `planned` | 版本已规划，任务待拆分 | 否 |
| `in_progress` | 版本任务执行中 | 否 |
| `done_e2e` | 端到端验收通过，尚未做 Release QA | 否 |
| `qa_passed` | Release QA 审计通过 | 允许进入发布同步 |
| `qa_failed` | Release QA 发现阻塞问题 | 否 |
| `blocked` | 缺少证据、凭据或环境能力 | 否 |
| `release_complete` | finalize 成功 + 版本文档同步完成 | 已发布 |

`qa_failed` 与 `blocked` 禁止任何形式的 finalize 或公开发布声明。

## 发布门禁顺序

1. 所有任务 `verified_complete`；
2. 端到端集成验收通过（`done_e2e`）；
3. Release QA 审计通过，`docs/qa/versions/<ver>/QA-审计报告.md` 结论为 `qa_passed`；
4. 版本文档（`规划需求.md`、`更新日志.md`，可选 `技术方案.md`）已同步；
5. tag 打在已验证的提交上；
6. 必要签名/公证凭据与环境就绪，否则 `blocked`。

## 里程碑索引

> 按实际版本填写。状态必须与 `docs/versions/<ver>/更新日志.md` 和 `docs/qa/versions/<ver>/QA-审计报告.md` 一致。

| 版本 | 状态 | 发布日期 | QA 报告 |
|---|---|---|---|
| <VERSION> | <STATUS> | <DATE_OR_UNRELEASED> | <QA_REPORT_LINK_OR_NONE> |
