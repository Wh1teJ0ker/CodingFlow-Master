# 00 — 文档规划规范

> 本过程文件融合自原 `project-doc-planning` skill。主会话在用户要求建立/梳理/优化项目文档体系时阅读本文件。

## 适用场景

- 用户说"先设计需求文档""应该有哪些文档""帮我规划 docs 目录"。
- 用户要做一个新项目，但不想一开始引入过重的软件工程流程。
- 用户希望把当前需求先收敛成 MVP，再进入实现。
- 用户需要一套能直接指导编码的文档，而不是泛泛而谈的模板。
- 用户项目偏小到中型，尤其是桌面应用、本地优先工具、单机应用、个人产品、内部工具。

目标不是生成"流程很全但没人会看的大而全文档体系"，而是先建立一套可以直接服务设计、开发、验收的最小文档闭环。

## 默认输出原则

除非用户明确要求其他语言或格式，否则默认：
- 输出中文。
- 文档写入 `docs/` 目录（保存性文档，不删除）。
- 先建立文档体系，再逐步补全文档内容。
- 以当前对话中明确过的需求为准，不擅自扩展远期需求。
- 如果用户说"当前需求就是 MVP"，则所有文档都必须围绕 MVP 边界组织。

## 推荐文档体系

默认推荐以下四份核心文档（编号即文件名前缀）：

1. `00-需求文档.md`
2. `01-页面与交互说明.md`
3. `02-技术设计文档.md`
4. `03-开发任务清单.md`

此外，**项目根目录必须提供双语 README**（`README.md` 中文主版 + `README_EN.md` 英文版）。README 默认按“轻量仓库入口文档”编写，重点写项目介绍、当前状态、Quick Start、基本验证和必要链接；详细设计、长篇说明和扩展文档优先放到 `docs/`。编写依据见 [`references/README-STANDARD.md`](./references/README-STANDARD.md)，可从 [`assets/project-templates/README.md.template`](./assets/project-templates/README.md.template) 与 [`assets/project-templates/README_EN.md.template`](./assets/project-templates/README_EN.md.template) 复制后填充。

对于大多数小中型项目，这四份文档已足够覆盖：
- 做什么 / 不做什么
- 界面和交互怎么表现
- 技术上如何拆分职责
- 实际开发时先做什么、后做什么

如果用户没有明确要求更复杂的流程，不要主动引入额外的 PRD、概要设计、详细设计、测试计划、上线手册、运维手册等整套重流程文档。

## 版本化文档体系

当项目需要分版本/里程碑交付时，在 `docs/` 下增加：

```text
docs/
├── 00-需求文档.md           # 全景需求、MVP 边界、验收标准
├── 01-页面与交互说明.md      # 全局页面结构、状态流、交互规则
├── 02-技术设计文档.md        # 总技术架构、模块职责、关键数据流
├── 03-开发任务清单.md        # 全局任务清单、阶段、依赖、验收方式
├── 04-版本标准.md           # 版本号语义、里程碑索引、版本状态口径
├── versions/
│   └── <ver>/
│       ├── 规划需求.md       # 本版本范围、目标、任务清单
│       ├── 技术方案.md       # 本版本技术方案；简单版本可选
│       └── 更新日志.md       # 本版本进度表；必须与 handoff/TASK-BOARD.md 对齐
└── qa/
    └── versions/
        └── <ver>/
            └── QA-审计报告.md # 本版本发布前全局审计报告；与 versions/<ver>/ 同步
```

**关键规则**：`versions/<ver>/更新日志.md` 的进度表必须与 `handoff/TASK-BOARD.md` 的任务状态保持一致。每个 `versions/<ver>/` 必须同步规划对应的 `qa/versions/<ver>/QA-审计报告.md`；版本完成 / 发布状态只能在该 QA 报告已落盘且结论为 `qa_passed` 后写入。详见 [`06-progress-sync.md`](./06-progress-sync.md) 与 [`07-release-qa-audit.md`](./07-release-qa-audit.md)。

版本文档（`04-版本标准.md`、`versions/<ver>/规划需求.md`、`versions/<ver>/更新日志.md`、`qa/versions/<ver>/QA-审计报告.md`）可从 [`assets/project-templates/release-docs/`](./assets/project-templates/release-docs/) 复制模板后填充。

## 四份文档的职责边界

### 1. 需求文档（00-需求文档.md）

回答：这个项目要做什么？当前版本先做什么？明确不做什么？核心功能有哪些？验收标准是什么？

必须强调边界：
- 项目目标
- MVP 范围
- 非目标 / 本期不做
- 功能需求
- 非功能需求
- 验收标准

如果用户对"范围蔓延"有担心，要把"当前需求默认即 MVP"写清楚。

### 2. 页面与交互说明（01-页面与交互说明.md）

回答：界面有哪些区域？每个按钮或入口做什么？页面状态怎么切换？用户的主流程是什么？异常和确认动作怎么处理？

不是视觉稿说明，而是交互行为说明。优先覆盖：
- 页面分区
- 工具栏 / 侧栏 / 主内容区 / 状态栏
- 空状态、加载状态、错误状态、未保存状态
- 主流程：打开、切换、保存、返回、确认
- 对长文档、大列表、失败场景的交互约束

### 3. 技术设计文档（02-技术设计文档.md）

回答：为什么这么分层？前后端或各模块怎么分工？状态如何组织？关键数据结构是什么？核心命令、接口、数据流是什么？性能和错误处理大方向是什么？

优先写清：架构分层、模块职责、数据模型、状态结构、命令/接口、关键数据流、性能策略、错误处理与边界。不要一开始过度细化到实现代码级别。

### 4. 开发任务清单（03-开发任务清单.md）

回答：实现时先做什么、后做什么？哪些任务相互依赖？每个任务完成后怎么验证？阶段性里程碑怎么定义？

必须可执行，不能只是泛泛 checklist。优先拆出：阶段划分、任务目标、前置依赖、建议修改位置、实现要点、交付物、验收标准、验证方式、风险点。

## 写作口径要求

- 先边界，后细节。
- 先 MVP，后扩展。
- 先主流程，后边缘能力。
- 先用户可感知行为，后实现细节。
- 先可执行任务，后抽象愿景。

避免：需求文档写成产品宣传；页面文档只写"有个按钮"不写状态和行为；技术设计只有技术栈没有模块职责和数据流；开发任务清单只有一句话任务名没有依赖和验收；把未来构想混进 MVP 导致范围失控。

## 针对 MVP 的特殊处理

如果用户强调"当前需求就是最小 MVP"：
- 在需求文档前部写清"当前文档中的需求默认即本项目第一期 MVP 范围"。
- 单列"本期必须实现"和"本期明确不做"。
- 在页面文档中优先覆盖主流程和高频状态，不为远期能力预留过多复杂交互。
- 在技术设计中优先选择简单稳定的架构，不为未来扩展过度设计。
- 在开发任务清单中按 MVP 交付链路拆任务，而不是按理想化大系统拆任务。

## 交付质量检查清单

在结束前，至少检查：
- 四份文档是否围绕同一个 MVP 边界？
- 需求文档是否明确"做什么 / 不做什么 / 如何验收"？
- 页面文档是否覆盖主状态、主流程和异常处理？
- 技术设计是否有模块边界、状态结构、关键数据流？
- 开发任务清单是否包含依赖、阶段、验收和验证方式？
- 文档之间是否存在术语或范围冲突？
- 是否遗漏了用户明确提出过的功能点？

## 何时不要过度发挥

以下情况下不要擅自加复杂文档：
- 用户明确说"可以适当简化"。
- 用户是个人项目或小团队项目。
- 用户只是想尽快开始实现。
- 用户没有要求测试计划、发布计划、运维体系。

这时宁可把四份核心文档写扎实，也不要额外增加很多低使用率文档。
